# Nexus Platform 修改说明明细

更新日期：2026-04-14

本文档用于记录本次提交的实际改动范围，便于联调、测试与回归验证。

## 1. 修改背景

本次改动聚焦 iOS 客户端「发现页 -> 游戏详情页」链路的稳定性与后端数据对齐能力，主要目标：

1. 发现页顶部运营位点击时，确保能精准跳转到后端配置的 `appId` 对应游戏。
2. 当运营位配置异常（无效 `appId`）时，保证页面仍可用并自动回退。
3. 游戏详情页接入后端 `runtime-profile`，展示更贴近线上运营配置的信息。

## 2. iOS 客户端改动说明

### 2.1 发现页 Hero 精准跳转与降级兜底

改动目的：
- 解决 Hero 卡片点击目标可能与后端配置不一致的问题。
- 避免运营配置错误导致用户点击后无可用详情页。

主要改动：
- `DiscoverViewModel` 新增 `heroGame` 解析逻辑：
  - 先在已加载游戏列表中按 `appId` 匹配。
  - 若未命中，则调用单游戏接口补拉目标游戏。
- 当 `appId` 无效时，自动回退到可玩游戏（优先排行榜首项，其次游戏列表首项）。
- 新增 `heroFallbackMessage`，用于提示已触发回退。
- `DiscoverView` 增加回退提示条，并统一 Hero 跳转目标选择逻辑。

涉及文件：
- `ios-client/NexusPlatform/Sources/NexusPlatform/Features/Discover/DiscoverViewModel.swift`
- `ios-client/NexusPlatform/Sources/NexusPlatform/Features/Discover/DiscoverView.swift`

改动效果：
- Hero 点击路径可稳定落到有效游戏详情。
- 运营配置异常时前台可自恢复，不阻塞用户继续体验。

### 2.2 游戏目录服务补充单游戏查询能力

改动目的：
- 支撑发现页 Hero 的精准目标补拉。

主要改动：
- 在 `GameCatalogServiceProtocol` 中新增 `fetchGame(appID:)`。
- `GameCatalogService` 新增 `/game/{appId}` 请求及字段归一化逻辑。

涉及文件：
- `ios-client/NexusPlatform/Sources/NexusPlatform/Core/Network/GameCatalogService.swift`

改动效果：
- iOS 端可按 `appId` 单独拉取游戏详情基础信息，减少前端猜测映射。

### 2.3 游戏详情页接入 Runtime Profile

改动目的：
- 详情页展示与后端运营配置一致的 runtime 信息。

主要改动：
- 新增 `GameRuntimeProfileService` 与 `RuntimeProfile` 模型，请求：
  - `GET /game/{appId}/runtime-profile`
- 新增 `GameDetailViewModel` 负责异步加载 runtime profile。
- `GameDetailView` 优先使用 runtime profile 的标题、副标题、厂牌、玩家数和 banner 图；无数据时自动回退到基础 `Game` 字段。

涉及文件：
- `ios-client/NexusPlatform/Sources/NexusPlatform/Core/Network/GameRuntimeProfileService.swift`
- `ios-client/NexusPlatform/Sources/NexusPlatform/Features/GameDetail/GameDetailViewModel.swift`
- `ios-client/NexusPlatform/Sources/NexusPlatform/Features/GameDetail/GameDetailView.swift`

改动效果：
- 详情页内容更贴合运营侧配置，视觉与信息层级更完整。
- 在 profile 缺失场景下仍可保持可浏览、可秒开。

## 3. 验证情况

已执行：
- `swift package --package-path ios-client dump-package`

结论：
- iOS Package 清单解析通过，本次变更文件可被工程正确识别。

## 4. 回归建议

1. 在运营后台分别配置有效与无效 Hero `appId`，验证发现页点击与回退提示行为。
2. 校验游戏详情页在有/无 `runtime-profile` 数据时的展示降级是否符合预期。
3. 重点回归“立即秒开”按钮链路，确认未受本次详情页数据源调整影响。
