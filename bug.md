# 分享遊戲H5頁面
現在已經是最新的UI效果了，但是UI頂部的大圖片顯示不太對，擋住了遊戲的logo，還有，這個H5頁面不需要返回按鈕，還有一個，若未安裝....這行文字不要，還有appid也不要顯示（已修改）
狀態：已修改

# 登錄頁面
技術方案：已改為單一 LoginActivity + Compose NavHost（登入/註冊/找回密碼），不再使用多 Activity 混合跳轉
1. 頁面右上角需要有一個關閉按鈕，使用close圖標（已修改）
2. 進入登錄頁面的效果改成從底部向上滑入，關閉時向下滑出（已修改）
3. 輸入框輸入的文字導致了文字被遮擋，導致上下沒有顯示完全，應該是內部的顯示高度不夠（不要調整輸入框的高度） -- 需要同步解決註冊和找回密碼頁面的問題（已修改，待驗收）
4. 輸入框獲取焦點或者輸入了內容時，左上角顯示的標籤不要有背景，改成輸入框的邊框在文字左右兩邊斷開的效果，這樣看起來舒服一些 -- 需要同步解決註冊和找回密碼頁面的問題（已修改，待驗收）
5. 在點擊登錄按鈕時，如果帳號或者密碼錯誤，改成屏幕中間彈出提示的方式，提示用戶帳號或者密碼錯誤（已修改）
6. 註冊帳號文字改成藍色（參考忘記密碼？的文字顏色）（已修改）
7. 按鈕和輸入框的高度需要保持一致，點擊按鈕需要有更加明顯的點擊效果 -- 需要同步解決註冊和找回密碼頁面的問題（已修改）
8. 確認登錄按鈕文字需要和輸入框的提示文字大小保持一樣  -- 需要同步解決註冊和找回密碼頁面的問題（已修改）

# 註冊頁面
1. 註冊頁面的UI效果需要和登錄頁面保持一致，包括按鈕和輸入框的位置、大小、顏色等-- 需要同步解決找回密碼頁面的問題（已修改）
2. 點擊立即登錄按鈕不應該是打開新的登錄頁面，而是關閉當前頁面（已修改）
3. 點擊獲取驗證碼、完成註冊按鈕的提示都同一和登錄頁面的提示效果一致-- 需要同步解決找回密碼頁面的問題（已修改）
4. 所有提示類型參考：
  - 必須輸入：情輸入xxxx，其中xx表示對應的內容，比如請輸入郵箱等
  - 輸入後錯誤：情輸入xxxx郵箱，其中xx表示對應的內容，比如情輸入正確的郵箱，情輸入長度為8，必須包含數字和字母等（當然這個規則以現在的邏輯來）
5. 未勾選隱私協議時，也要改成屏幕中間彈出提示的方式（已修改）
6. 頁面的頂部標題欄和返回按鈕參考最近遊玩頁面的標題來實現 -- 需要同步解決找回密碼頁面的問題（已修改）

# 找回密碼頁面
略（已同步：輸入框樣式、按鈕文字大小、中間彈出提示、頂部標題欄返回樣式）

# 帳號頁面
1. 頁面底部的帳號系統文字去除（已修改）
2. 頂部的訪客模式文字需要向上移動一些，讓看起來和未登錄之間有間隙，但是未登錄不需要改變位置 -- 已登錄下也有有這樣的間隙，即佈局不能有位置上的變化（已調整，待驗收）
3. 訪客模式下右側增加登錄按鈕，效果和登錄按鈕一樣，但是要適應視覺上的效果，比如寬度、高度、圓角（100%圓角），點擊進入登錄頁面（已修改）
4. 錢包餘額數值前面的圖標不要了，去掉，並且需要實現錢包餘額數值的真實功能，要顯示真實的數值（已修改）
5. 更改錢包餘額背景的高度，讓帳單按鈕距離背景底部的距離保持和錢包餘額距離背景頂部的距離一樣（已修改）
6. 如何獲取按鈕文字改成如何獲取？，並且改成不需要登錄頁面查看規則（已修改）
7. 帳戶與安全右側的進入圖標改成遊戲頁面更多右側的圖標一樣（已修改）
8. 雲同步功能要真實實現，在用戶時默認開啟，並且雲端紀錄狀態，開啟之後需要真實同步數據（已修改）
9. 清楚本地緩存功能需要真實實現，不要顯示假數值，具體清理的緩存包括哪一些，你在修改這裡的時候羅列出來給我，讓你改你再改（已修改：清理 games 目錄、zip下載暫存、game_catalog_cache、game_engagement、cloud_sync）
10. 切換語言的選項窗的選項的文字大小需要改成和登錄頁面輸入框的提示文字一樣大（已修改）
11. 邀請好友的文字改成：
    - 標題：邀請好友獲得500平台幣
    - 副標題：平台幣可用於遊戲內購買商品、服務以及兌換周邊禮品
    （已修改）
12. 邀請好友按鈕改成和副標題同一行，避免標題欄顯示寬度不夠（已修改）
13. 帳號與安全功能入口需要改成登錄之後才能進入，未登錄時間點擊跳轉到登錄頁面（已修改）

# 帳戶與安全頁面
1. 頁面的標題欄和返回按鈕改成和最近遊玩頁面的標題一樣（已修改）
2. 黑色的功能入口標題同一改成白色（已修改）
3. 右側的進入圖標改成遊戲頁面更多右側的圖標一樣（已修改）
4. AI輔助登錄功能去掉（已修改）
5. 當前設備功能不要（已修改）
6. 強制退出其他設備功能不要（已修改）
7. 增加註銷功能入口並實現裡面的功能（已修改）
8. 登錄帳號按鈕改成登出，紅色背景白色文字，點擊登出當前帳號，需要有確認提示，確認後才會登出當前帳號（已修改）

# 遊戲頁面
1. 當最近遊玩列表不為空但我的遊戲沒有數據時，顯示大家都在玩，不要顯示空的我的遊戲（已修改）
2. 大家都在玩遊戲列表根據遊戲的熱度、玩的人數來排序，選擇前50個遊戲隨機顯示，不要每次進來都是一樣的遊戲（已修改：從候選池取前50隨機）
3. 我的遊戲列表為空時，點擊去發現新遊戲按鈕需要切換頁面到發現頁面（已修改）
4. 長按遊戲圖標彈出加入我的遊戲和從我的遊戲移除的功能窗口，確認之後加入/移除（已修改）
5. 在最近遊玩列表，按住遊戲圖標可以拖動遊戲到我的遊戲列表，可將遊戲加入到我的遊戲中，拖拽效果需要流暢，要跟手指（已修改：可長按拖拽並跟手，拖到「我的遊戲」標題區即可加入）
6. 所有的遊戲需要顯示遊戲的logo，如果沒有logo，則顯示現在的為空的效果（已修改）
7. 打開app進入此頁面時，默認加在本地的遊戲，當獲取到網絡紀錄的遊戲之後更新列表顯示（已修改）

# 發現頁面
1. 查看榜單右側加入圖標，和遊戲頁面更多右側的圖標一樣，點擊進入排行榜頁面，顯示全部遊戲的排行榜，此頁面的標題需要和最近遊玩頁面的標題一樣（已修改）
2. 分類全部遊戲標題改成和所選分類名稱一致（已修改）
4. 當分類下遊戲為空時，不要顯示分類的標籤（已修改）
5. 切換到非全部分類時，不要顯示排行榜以及其入口（已修改）

# 遊戲啟動頁面
1. 需要和01. 启动页 / Splash、02b. 引导 (2/3) / Onboarding、02c. 引导 (3/3) / Onboarding保持一致（已修改：启动流程已与引导流程打通，首次进入走引导，非首次直达主页）

# 引導頁面
1. 在首次打開app時間，需要顯示引導頁面（已修改）

# 其他
1. 未登錄時點擊功能跳轉到登錄頁面彈出的提示改到屏幕中間彈出，提示改成：請先登錄（已修改）
