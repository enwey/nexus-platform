# Nexus Platform 修改说明明细

本文档用于说明本次在 `nexus-platform` 项目中已落地的主要修改内容，方便开发、测试、提测、上线和后续交接时快速了解改动范围。

## 1. 修改背景

本次修改主要分为两部分：

1. 后端 `P0` 安全与可运维性整改。
2. 基于 `android-client` 能力，为后台管理 Web 补充对应的 Android 管理台功能。

本次目标不是只修单个问题，而是同时处理以下几类风险：

- 认证鉴权方式过于脆弱，依赖拦截器和路径判断，扩展性和安全性不足。
- 主配置中存在不安全默认值，环境变量约束不够严格。
- 缺少数据库迁移基线，部署与初始化一致性不足。
- Android 宿主能力虽然已在原生端实现，但后台缺少统一可视化管理入口。

## 2. 后端 P0 改动说明

### 2.1 认证鉴权体系重构

改动目的：
- 用标准的 Spring Security + JWT 替代原先的拦截器式鉴权。
- 将“是否有权限”从字符串路径判断，升级为统一的认证上下文和方法级权限控制。

主要改动：
- 新增 `SecurityConfig`，统一配置认证链路、无状态会话、公开接口和受保护接口。
- 新增 `JwtAuthenticationFilter`，从 `Authorization: Bearer <token>` 中解析用户身份。
- 新增统一的 401/403 处理器，避免默认 HTML 错误页或不一致响应。
- 开启 `@EnableMethodSecurity`，允许控制器使用 `@PreAuthorize` 做权限控制。
- 删除旧的 `AuthInterceptor` 方案，并从 `WebConfig` 中移除旧拦截器注册。

涉及文件：
- `backend/src/main/java/com/nexus/platform/config/SecurityConfig.java`
- `backend/src/main/java/com/nexus/platform/security/JwtAuthenticationFilter.java`
- `backend/src/main/java/com/nexus/platform/security/ApiAuthenticationEntryPoint.java`
- `backend/src/main/java/com/nexus/platform/security/ApiAccessDeniedHandler.java`
- `backend/src/main/java/com/nexus/platform/config/WebConfig.java`
- `backend/src/main/java/com/nexus/platform/config/AuthInterceptor.java`（已删除）

改动效果：
- 后端接口的认证入口更统一。
- 更容易继续扩展角色、权限、审计和接口分级控制。
- 减少“新增接口后忘记加拦截”的风险。

### 2.2 控制器权限判断改为方法级权限控制

改动目的：
- 避免控制器依赖 `request attribute` 取当前用户。
- 权限校验逻辑集中到 Spring Security 和 `RolePermissionService`。

主要改动：
- 多个控制器改为通过 `@AuthenticationPrincipal User` 获取当前用户。
- 在关键接口上增加 `@PreAuthorize(...)`，显式绑定权限点。
- `RolePermissionService` 新增基于 `Authentication` 的权限判断重载。

涉及文件：
- `backend/src/main/java/com/nexus/platform/controller/UserController.java`
- `backend/src/main/java/com/nexus/platform/controller/WalletController.java`
- `backend/src/main/java/com/nexus/platform/controller/BillingController.java`
- `backend/src/main/java/com/nexus/platform/controller/ReferralController.java`
- `backend/src/main/java/com/nexus/platform/controller/LibraryController.java`
- `backend/src/main/java/com/nexus/platform/controller/AuditController.java`
- `backend/src/main/java/com/nexus/platform/controller/AndroidAdminController.java`
- `backend/src/main/java/com/nexus/platform/controller/OpsDiscoverAdminController.java`
- `backend/src/main/java/com/nexus/platform/controller/OpsGameProfileAdminController.java`
- `backend/src/main/java/com/nexus/platform/controller/GameController.java`
- `backend/src/main/java/com/nexus/platform/security/RolePermissionService.java`

改动效果：
- 当前用户获取方式更标准。
- 权限边界更清晰，后续做接口梳理和安全审计更方便。

### 2.3 安全默认值收紧

改动目的：
- 避免项目在未提供生产级环境变量时使用弱默认配置启动。
- 降低“误把本地默认值带到测试/正式环境”的风险。

主要改动：
- 数据库连接改为强依赖环境变量注入。
- MinIO、Nakama、JWT Secret 改为从环境变量读取。
- 默认关闭 `allow-insecure-defaults`。
- 默认关闭 `bootstrap-admin`。
- 移除弱口令默认值。

涉及文件：
- `backend/src/main/resources/application.yml`

改动效果：
- 项目启动时对环境配置要求更严格。
- 部署行为更可控，更适合多环境切换和一键部署场景。

### 2.4 启动时安全自检增强

改动目的：
- 在应用启动阶段尽早发现危险配置，而不是上线后才出问题。

主要改动：
- 校验 JWT Secret 是否为空或过短。
- 仅在启用 bootstrap admin 时校验其密码。
- 明确禁止默认不安全模式在非预期情况下启用。

涉及文件：
- `backend/src/main/java/com/nexus/platform/config/SecuritySanityCheck.java`

改动效果：
- 可以更早拦截配置错误。
- 避免“应用虽然启动了，但其实是危险配置”的情况。

### 2.5 密码加密方式统一

改动目的：
- 避免不同服务中直接 `new BCryptPasswordEncoder()`，导致实现分散。

主要改动：
- 统一改为注入 `PasswordEncoder` Bean。

涉及文件：
- `backend/src/main/java/com/nexus/platform/config/AdminBootstrapRunner.java`
- `backend/src/main/java/com/nexus/platform/service/UserService.java`
- `backend/src/main/java/com/nexus/platform/service/AccountOpsService.java`

改动效果：
- 密码编码行为更一致。
- 后续如需替换算法或统一加盐策略会更容易。

### 2.6 全局异常信息收敛

改动目的：
- 防止服务端异常细节直接暴露给前端。

主要改动：
- 通用异常改为服务端记录日志，前端返回固定文案 `Internal server error`。
- 保留参数类异常的业务可读性。

涉及文件：
- `backend/src/main/java/com/nexus/platform/config/GlobalExceptionHandler.java`

改动效果：
- 降低内部堆栈和异常信息泄露风险。

### 2.7 Flyway 基线迁移补齐

改动目的：
- 让数据库结构演进从“依赖 JPA 自动改表”转向“可追踪的迁移脚本”。

主要改动：
- 增加基线迁移脚本，覆盖当前核心业务表。
- JPA 配置从 `ddl-auto: update` 收紧为 `ddl-auto: validate`。
- 开启 Flyway。

涉及文件：
- `backend/src/main/resources/db/migration/V1__baseline_schema.sql`
- `backend/src/main/resources/application.yml`

改动效果：
- 后续部署、回滚、环境初始化更规范。
- 更适合 CI/CD 和多环境数据库一致性管理。

### 2.8 本地开发脚本与部署文档同步调整

改动目的：
- 让本地启动脚本和部署说明与新的安全配置模型保持一致。

主要改动：
- 本地脚本改为注入必要环境变量。
- 示例环境变量文件补充新的配置项。
- 生产 Compose 不再依赖硬编码默认值。

涉及文件：
- `.env.example`
- `scripts/env-local.ps1`
- `scripts/env-local.cmd`
- `scripts/check-env.ps1`
- `scripts/start-local.ps1`
- `DEPLOYMENT.md`
- `docker-compose/docker-compose-prod.yml`

改动效果：
- 本地和部署环境的行为更接近真实生产模式。
- 对后续“一键部署”目标更友好。

## 3. Android 后台管理功能改动说明

### 3.1 修改背景

`android-client` 中已经实现了不少宿主能力，例如：

- WebView 安全设置
- Async / Sync Bridge
- 资源加载与更新能力
- 本地存储
- 原生网络代理
- 剪贴板、震动等设备能力

但此前后台 Web 侧只存在一个偏“混合配置页”的入口，无法完整表达 Android 宿主的实际能力范围，也不方便运营、测试和开发统一查看。

### 3.2 后端 Android 管理接口增强

改动目的：
- 让后台能拿到更完整的 Android 宿主概览数据。

主要改动：
- 扩展 Android 管理 DTO，新增：
  - 运行时就绪游戏数
  - Android 宿主能力列表 `hostCapabilities`
  - 更完整的 Bridge 能力矩阵
- `AndroidAdminService` 补充：
  - 宿主能力清单
  - 更贴近真实实现的 Bridge API 状态
  - 游戏资源是否达到 Android 运行时就绪状态

涉及文件：
- `backend/src/main/java/com/nexus/platform/dto/AndroidAdminDtos.java`
- `backend/src/main/java/com/nexus/platform/service/AndroidAdminService.java`
- `backend/src/main/java/com/nexus/platform/controller/AndroidAdminController.java`

改动效果：
- 后台可以直接看到 Android 宿主“已经做了什么”和“还只是 mock 什么”。
- 方便测试和产品判断当前 Android 宿主可交付程度。

### 3.3 新增独立 Android 管理台页面

改动目的：
- 为 Android 宿主提供专门的后台页面，而不是继续混在 Runtime Ops Console 中。

主要改动：
- 新增 `AndroidConsole.vue` 页面。
- 页面内容包括：
  - Android 运行时摘要
  - 宿主能力地图
  - 运行时配置管理
  - Bridge 能力矩阵
  - 游戏资源就绪状态
- 支持筛选、搜索和状态快速识别。

涉及文件：
- `ops-portal/src/views/AndroidConsole.vue`

改动效果：
- Android 后台能力从“单页配置表单”升级为“可视化管理台”。
- 更适合开发、测试、运维、产品共同使用。

### 3.4 路由与导航补充

改动目的：
- 让新增 Android 管理台可以直接从现有后台入口访问。

主要改动：
- 新增 `/android-console` 路由。
- `/android` 重定向到新的 Android 管理台。
- 在审核台、审计日志、Runtime Ops 页面增加入口按钮。

涉及文件：
- `ops-portal/src/router/index.js`
- `ops-portal/src/views/Audit.vue`
- `ops-portal/src/views/AuditLogs.vue`
- `ops-portal/src/views/RuntimeOpsConsole.vue`

改动效果：
- 后台页面导航更完整，Android 管理台与现有运营后台形成联动。

## 4. 验证情况

本次改动已完成的基础验证：

- `backend` 编译通过。
- `ops-portal` 构建通过。

说明：
- 本次主要完成了代码级和构建级验证。
- 尚未在真实生产数据库上做 Flyway 基线比对验证。
- 尚未执行完整自动化测试和端到端业务回归测试。

## 5. 对项目的实际影响

### 正向影响

- 认证、权限、异常处理、安全默认值整体更加规范。
- 数据库演进路径更适合部署和长期维护。
- Android 宿主能力与后台管理打通，方便后续持续扩展。
- 一键部署、多环境切换所需的配置抽象更完整。

### 需要注意的点

- 旧环境如果仍依赖历史默认配置，需要同步补齐环境变量。
- Flyway 基线脚本需要和现有真实数据库结构再做一次核对。
- Android 管理台当前有部分中文文本编码异常，后续建议统一检查前端文件编码并修正展示文本。

## 6. 建议的下一步

建议后续继续推进以下事项：

1. 跑完整后端测试和关键接口回归测试。
2. 对 Flyway 基线与现网数据库结构做一次正式比对。
3. 继续推进 `P1` 级别架构优化，例如：
   - 大文件处理优化
   - 对象存储下载链路优化
   - 更细粒度的监控与缓存落地
4. 修复 Android 管理台页面中的中文编码问题，并补充提测截图。

## 7. 一句话总结

本次修改的本质，是把 `nexus-platform` 从“能跑的项目骨架”进一步推进到“更安全、更可部署、更可管理”的状态，并补上了 Android 宿主能力在后台侧的对应管理能力。
