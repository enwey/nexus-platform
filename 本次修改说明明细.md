# Nexus Platform 修改说明明细

更新日期：2026-04-10

本文档用于说明本次在 `nexus-platform` 项目中已落地的主要修改内容，方便开发、测试、提测、上线和后续交接时快速了解改动范围。

> 注：本文件将持续更新，记录每次重要修改的内容。

## 1. 修改背景

本次修改主要集中在以下几个方面：

1. Android 客户端的认证和用户体验优化。
2. 后端服务的功能增强和 API 调整。
3. 前端管理台的功能完善和国际化支持。

本次目标是提升平台的整体用户体验和功能完整性，确保各端之间的协同工作更加顺畅。

## 2. Android 客户端改动说明

### 2.1 认证流程优化

改动目的：
- 提升用户登录、注册和密码找回的体验。
- 优化认证相关的 API 调用和错误处理。

主要改动：
- 重构 `LoginUseCase`，优化登录逻辑和错误处理。
- 改进 `AuthRepository`，统一认证相关的 API 调用。
- 优化 `BackendAuthApi`，调整认证相关的接口定义。
- 改进 `LoginActivity`、`RegisterActivity` 和 `ForgotPasswordActivity` 的用户界面和交互流程。

涉及文件：
- `android-client/app/src/main/java/com/nexus/platform/data/remote/BackendAuthApi.kt`
- `android-client/app/src/main/java/com/nexus/platform/data/repository/AuthRepository.kt`
- `android-client/app/src/main/java/com/nexus/platform/domain/usecase/LoginUseCase.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/auth/ui/ForgotPasswordActivity.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/auth/ui/LoginActivity.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/auth/ui/LoginViewModel.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/auth/ui/RegisterActivity.kt`

改动效果：
- 认证流程更加顺畅，错误提示更加清晰。
- 用户体验得到提升，减少了登录和注册过程中的困惑。

### 2.2 游戏运行时和库功能优化

改动目的：
- 提升游戏运行时的性能和稳定性。
- 优化游戏库的展示和管理功能。

主要改动：
- 改进 `GameManager`，优化游戏的加载和管理逻辑。
- 优化 `GameRuntimeActivity`，提升游戏运行时的用户体验。
- 改进 `LibraryScreen`，优化游戏库的展示和交互。
- 调整 `GameItem` 模型，增强游戏信息的完整性。

涉及文件：
- `android-client/app/src/main/java/com/nexus/platform/domain/model/GameItem.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/game/data/GameManager.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/game/runtime/GameRuntimeActivity.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/library/ui/LibraryScreen.kt`

改动效果：
- 游戏运行更加流畅，用户体验得到提升。
- 游戏库的管理和展示更加直观。

### 2.3 API 能力增强

改动目的：
- 扩展 Android 客户端的 API 能力，支持更多功能。
- 优化 API 调用的安全性和可靠性。

主要改动：
- 扩展 `NetworkApis.kt`，增加网络相关的 API 能力。
- 扩展 `OtherApis.kt`，增加其他功能的 API 能力。
- 扩展 `SystemApis.kt`，增加系统相关的 API 能力。
- 改进 `PlatformBackendApi.kt`，优化与后端的通信。

涉及文件：
- `android-client/app/src/main/java/com/nexus/platform/core/bridge/api/NetworkApis.kt`
- `android-client/app/src/main/java/com/nexus/platform/core/bridge/api/OtherApis.kt`
- `android-client/app/src/main/java/com/nexus/platform/core/bridge/api/SystemApis.kt`
- `android-client/app/src/main/java/com/nexus/platform/data/remote/PlatformBackendApi.kt`

改动效果：
- Android 客户端的 API 能力更加丰富。
- 与后端的通信更加稳定和安全。

### 2.4 国际化支持优化

改动目的：
- 提升应用的国际化支持，确保在不同语言环境下的正确显示。

主要改动：
- 更新英文、简体中文和繁体中文的字符串资源文件。
- 确保所有用户界面元素都有正确的翻译。

涉及文件：
- `android-client/app/src/main/res/values-en/strings.xml`
- `android-client/app/src/main/res/values-zh-rCN/strings.xml`
- `android-client/app/src/main/res/values-zh-rTW/strings.xml`
- `android-client/app/src/main/res/values/strings.xml`

改动效果：
- 应用在不同语言环境下都能正确显示。
- 提升了国际用户的体验。

## 3. 后端服务改动说明

### 3.1 用户管理功能增强

改动目的：
- 优化用户管理相关的功能，提升用户体验。
- 增强用户认证和授权的安全性。

主要改动：
- 改进 `UserController`，优化用户相关的 API 接口。
- 优化 `UserService`，提升用户管理的功能和安全性。
- 改进 `AccountService` 和 `AccountOpsService`，增强账户管理能力。
- 调整 `UserRepository`，优化用户数据的存储和查询。

涉及文件：
- `backend/src/main/java/com/nexus/platform/controller/UserController.java`
- `backend/src/main/java/com/nexus/platform/repository/UserRepository.java`
- `backend/src/main/java/com/nexus/platform/service/AccountOpsService.java`
- `backend/src/main/java/com/nexus/platform/service/AccountService.java`
- `backend/src/main/java/com/nexus/platform/service/UserService.java`

改动效果：
- 用户管理功能更加完善和安全。
- API 接口更加符合 RESTful 设计规范。

### 3.2 游戏管理功能优化

改动目的：
- 提升游戏管理的功能和性能。
- 优化游戏相关的 API 接口。

主要改动：
- 改进 `GameService`，优化游戏的管理和分发逻辑。
- 调整 `Game` 实体，增强游戏信息的完整性。
- 优化 `DiscoverFeedItem` 和 `GameMetadataUpdateRequest` DTO，提升数据传输的效率。

涉及文件：
- `backend/src/main/java/com/nexus/platform/dto/DiscoverFeedItem.java`
- `backend/src/main/java/com/nexus/platform/dto/GameMetadataUpdateRequest.java`
- `backend/src/main/java/com/nexus/platform/entity/Game.java`
- `backend/src/main/java/com/nexus/platform/service/GameService.java`

改动效果：
- 游戏管理功能更加高效和可靠。
- 数据传输更加优化，提升了系统性能。

### 3.3 Android 管理功能增强

改动目的：
- 提升 Android 管理相关的功能，确保后台能够更好地管理 Android 客户端。

主要改动：
- 改进 `AndroidAdminService`，增强 Android 管理的功能和能力。

涉及文件：
- `backend/src/main/java/com/nexus/platform/service/AndroidAdminService.java`

改动效果：
- Android 管理功能更加完善。
- 后台能够更好地监控和管理 Android 客户端。

### 3.4 配置优化

改动目的：
- 优化应用的配置，提升系统的性能和安全性。

主要改动：
- 调整 `application.yml` 配置文件，优化系统的配置参数。

涉及文件：
- `backend/src/main/resources/application.yml`

改动效果：
- 系统配置更加优化，提升了性能和安全性。

## 4. 前端管理台改动说明

### 4.1 API 调用优化

改动目的：
- 优化前端的 API 调用，提升系统的性能和可靠性。

主要改动：
- 改进 `request.js` 文件，优化 API 调用的错误处理和重试机制。
- 调整 `index.js` 文件，优化 API 接口的定义和管理。

涉及文件：
- `dev-portal/src/api/request.js`
- `ops-portal/src/api/index.js`
- `ops-portal/src/api/request.js`

改动效果：
- API 调用更加稳定和可靠。
- 错误处理更加完善，提升了用户体验。

### 4.2 国际化支持增强

改动目的：
- 提升前端管理台的国际化支持，确保在不同语言环境下的正确显示。

主要改动：
- 更新 `i18n.js` 文件，增加和优化国际化支持。

涉及文件：
- `dev-portal/src/i18n.js`
- `ops-portal/src/i18n.js`

改动效果：
- 前端管理台在不同语言环境下都能正确显示。
- 提升了国际用户的体验。

### 4.3 用户状态管理优化

改动目的：
- 优化前端的用户状态管理，提升系统的安全性和用户体验。

主要改动：
- 改进 `user.js` 文件，优化用户状态的管理和同步。

涉及文件：
- `dev-portal/src/stores/user.js`
- `ops-portal/src/stores/user.js`

改动效果：
- 用户状态管理更加稳定和安全。
- 提升了用户体验。

### 4.4 路由和视图优化

改动目的：
- 优化前端的路由和视图，提升系统的用户体验。

主要改动：
- 调整 `router/index.js` 文件，优化路由的定义和管理。
- 改进 `AndroidConsole.vue`、`Audit.vue`、`AuditLogs.vue` 和 `RuntimeOpsConsole.vue` 视图文件，提升用户界面和交互体验。

涉及文件：
- `ops-portal/src/router/index.js`
- `ops-portal/src/views/AndroidConsole.vue`
- `ops-portal/src/views/Audit.vue`
- `ops-portal/src/views/AuditLogs.vue`
- `ops-portal/src/views/RuntimeOpsConsole.vue`

改动效果：
- 路由管理更加清晰和高效。
- 用户界面更加美观和易用。

## 5. 验证情况

本次改动已完成的基础验证：

- 代码编译通过。
- 基本功能测试通过。

说明：
- 本次主要完成了代码级和构建级验证。
- 尚未执行完整的端到端测试和性能测试。

## 6. 对项目的实际影响

### 正向影响

- Android 客户端的用户体验得到提升。
- 后端服务的功能更加完善和安全。
- 前端管理台的功能更加丰富和易用。
- 国际化支持更加完善，提升了国际用户的体验。

### 需要注意的点

- 本次改动涉及多个模块，需要确保各模块之间的协同工作。
- 可能需要对现有数据进行迁移或调整，以适应新的功能和结构。

## 7. 建议的下一步

建议后续继续推进以下事项：

1. 执行完整的端到端测试，确保各模块之间的协同工作正常。
2. 进行性能测试，确保系统在高负载下的稳定性。
3. 收集用户反馈，进一步优化系统的用户体验。
4. 继续完善系统的功能和安全性，提升系统的整体质量。

## 8. 一句话总结

本次修改的本质，是通过优化 Android 客户端、后端服务和前端管理台的功能，提升 Nexus Platform 的整体用户体验和功能完整性，确保各端之间的协同工作更加顺畅。
## 6. 本次补充（再次補充）

补充日期：2026-04-10

本次未提交代码共 4 处，聚焦「最近游玩展示一致性」与「运营审核流转可见性」。

### 6.1 Android：最近游玩列表补强（服务端+本地合并）

改动目的：
- 解决用户已游玩但「最近游玩」列表偶发不显示的问题。
- 在服务端回包延迟或失败时，仍可基于本地记录稳定展示。

主要改动：
- 在 `LibraryViewModel` 中，当存在 `library/home` 数据时，不再仅依赖服务端 `recentGames/myGames`，改为与本地 `GameEngagementStore` 记录做去重合并。
- `currentPlayingGame` 回退链路增强：服务端当前游戏 -> 本地当前游戏 -> 最近游玩第一项。

涉及文件：
- `android-client/app/src/main/java/com/nexus/platform/feature/library/ui/LibraryViewModel.kt`

改动效果：
- 最近游玩和我的游戏列表显示更稳定，不再因为单次同步异常导致“玩过却不显示”。

### 6.2 Android：运行时游玩行为补同步

改动目的：
- 保证从运行时入口启动游戏时，服务端也能记录 PLAY 行为，减少客户端入口差异造成的数据丢失。

主要改动：
- 在 `GameRuntimeActivity.loadGame()` 内，写入本地游玩记录后，补发一次 `backendApi.markPlayed(game.id)`（best effort）。

涉及文件：
- `android-client/app/src/main/java/com/nexus/platform/feature/game/runtime/GameRuntimeActivity.kt`

改动效果：
- 最近游玩数据在本地与服务端之间一致性提升。

### 6.3 Ops Portal：审核台支持 DRAFT/REJECTED 提交审核

改动目的：
- 解决“开发者上传后运营后台看不到、无法进入审核流”的操作断点。

主要改动：
- 审核台列表纳入 `DRAFT` 状态展示（原先过滤掉）。
- 为 `DRAFT/REJECTED` 新增“提交审核（Submit For Review）”按钮。
- 新增前端 API：`submitGameForAudit(id, note)` 调用 `/game/submit/{id}`。

涉及文件：
- `ops-portal/src/views/Audit.vue`
- `ops-portal/src/api/index.js`

改动效果：
- 运营侧可直接把草稿/驳回状态送入待审（PENDING）流程，审核闭环更完整。

### 6.4 本次未提交文件清单

- `android-client/app/src/main/java/com/nexus/platform/feature/game/runtime/GameRuntimeActivity.kt`
- `android-client/app/src/main/java/com/nexus/platform/feature/library/ui/LibraryViewModel.kt`
- `ops-portal/src/api/index.js`
- `ops-portal/src/views/Audit.vue`

